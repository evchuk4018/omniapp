## Project Context

This project is currently in its initial setup stage. No features have been implemented yet.

## Implemented Features

None so far.

## Code Best Practices

- Keep functions focused on one responsibility.
- Prefer small, clear functions over large multi-purpose ones.
- Use descriptive names for files, functions, variables, and modules.
- Avoid unnecessary complexity.
- Keep logic easy to test, debug, and reuse.
- Write code that is readable before it is clever.

## File and Module Structure

- Keep files highly modular.
- Split large files when they begin handling multiple concerns.
- Group related logic together.
- Avoid mixing unrelated responsibilities in the same file.
- Prefer clear folder boundaries as the project grows.

## Documentation

- Keep nested documentation updated as the project changes.
- Update relevant README, module notes, or feature documentation when adding or changing functionality.
- Document new files, folders, and important architectural decisions.
- Remove outdated documentation when behavior changes.

## Development Expectations

- Implement only what is needed.
- Keep changes scoped and intentional.
- Maintain consistency with existing project structure.
- Add documentation alongside code changes.
- Do not list planned features as implemented until they exist in the codebase.

## Installed Skills Usage Policy

- Always use the matching installed skill whenever a request fits its purpose.
- Do not skip a matching skill; apply the skill first, then execute the task.
- If multiple skills match, combine them in a sensible order.
- Prefer `design-taste-frontend` over `design-taste-frontend-v1` unless backward compatibility is explicitly required.

### Skill Triggers (When To Use Each One)

- `brandkit`: Use for brand identity boards, logo systems, brand-guideline visuals, and visual-world presentation images.
- `design-taste-frontend`: Use for landing pages, portfolios, and frontend redesigns that need anti-generic, high-quality UI direction.
- `design-taste-frontend-v1`: Use only when exact v1 behavior is required for compatibility with existing workflows.
- `full-output-enforcement`: Use when tasks require complete, unabridged output and strict avoidance of placeholder or truncated responses.
- `gpt-taste`: Use for advanced UX/UI builds with heavy motion direction, strong AIDA structure, and GSAP-driven interactions.
- `high-end-visual-design`: Use when the goal is a premium, agency-grade visual finish (typography, spacing, components, motion quality).
- `imagegen-frontend-mobile`: Use to generate mobile app UI image concepts and multi-screen mobile flows (image output, not code).
- `imagegen-frontend-web`: Use to generate per-section website design reference images for landing/marketing/product web pages.
- `image-to-code`: Use when converting visual website references into implemented code with close visual fidelity.
- `industrial-brutalist-ui`: Use for raw mechanical / Swiss-terminal aesthetics, especially dashboards, portfolios, and editorial interfaces.
- `minimalist-ui`: Use for clean editorial interfaces with restrained color, flat bento structure, and minimal visual noise.
- `redesign-existing-projects`: Use when improving an existing app/site design without rewriting core functionality.
- `stitch-design-taste`: Use when producing `DESIGN.md` design-system guidance for Google Stitch-generated screens.
"""
